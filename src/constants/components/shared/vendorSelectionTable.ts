/**
 * Vendor Selection Table Constants
 * Classification: Confidential
 */

import { NUMBERMAP } from '@/constants/common';

// Status Labels
export const STATUS_LABELS = {
  PASS: 'Pass',
  FAIL: 'Fail',
} as const;

// Default Key Field
export const DEFAULT_KEY_FIELD = 'samples';

// Edit Icon Properties
export const EDIT_ICON = {
  SIZE: NUMBERMAP.EIGHTEEN,
  COLOR: '#666',
} as const;

