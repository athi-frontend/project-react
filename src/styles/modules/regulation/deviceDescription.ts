import { SxProps, Theme } from '@mui/material/styles'

export const DeviceDescriptionListSx: SxProps<Theme> = {
  minHeight: '293px',
  maxHeight: '293px',
  overflow: 'auto',
  display: 'inline-block',
  scrollbarWidth: 'none',
}